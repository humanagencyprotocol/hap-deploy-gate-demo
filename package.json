{
  "name": "hap-demo",
  "version": "0.1.0",
  "private": true,
  "description": "HAP Deploy Gate Demo - Reference implementation",
  "scripts": {
    "dev:server": "pnpm --filter @hap-demo/server dev",
    "dev:ui": "pnpm --filter @hap-demo/ui dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
